# Contributing

1. Fork the repository
2. Create feature branch
3. Make changes
4. Run `flake8` and `black` for linting
5. Submit Pull Request

## Adding New Checks

- Place in appropriate cloud folder (aws/, azure/, gcp/, kubernetes/)
- Follow existing naming convention
- Include docstrings
- Update README
